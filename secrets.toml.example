# Copy this file to secrets.toml (same folder) and put your real key there.
# secrets.toml is git-ignored — it will never be committed or pushed.
#
# For the deployed app, add the same line under:
#   Streamlit Cloud → your app → Settings → Secrets

GROQ_API_KEY = "gsk_your_key_here"
